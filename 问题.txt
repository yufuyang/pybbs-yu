问题
  1：话题和标签是多对多的关系 
              解决：将话题和标签改成一对多的关系  ok
  2：标签的创建是没有限制（谁都可以创建）
             解决：只能审核员和超级管理员创建（在权限表里添加标签添加权限，并给审核员和超级管理员添加权限，在标签controll里添加方法）
  3：创建话题时 标签时自由输入
             解决：更改为标签菜单 供其选择
  4：标签的管理时对所有管理开放的
             解决：在标签controll里相应方法中进行usertype判断 为2（超级管理员）可以进行操作，为1（审核员）从session中取出userid与标签表里的admin_id 进行比较，确定操作此方法
                      的管理员是创建此标签的管理
  5：话题的管理时对所有管理开放的
             解决： 先获取话题的标签， 得到标签的id，根据此id查找admin_id，与当前操作者的id匹配
  6: 标签与管理员没有建立关联
             解决：将 标签与管理员之间建立多对一的关系（目前在标签表里添加了admin_id字段）